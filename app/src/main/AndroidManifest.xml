<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.bdiw">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".bdiwbrowser.BdiwBrowser">

           <!--<intent-filter>-->
               <!--<action android:name="bdiwBrowser"/>-->
               <!--<category android:name="android.intent.category.DEFAULT"/>-->
               <!--<data android:scheme="http"/>-->
           <!--</intent-filter>-->

        </activity>
        <activity android:name=".main.activities.SignInActivity">
        </activity>
        <activity android:name=".communication.activities.ChatActivity">
        </activity>
        <activity android:name=".main.activities.SignUpActivity">
        </activity>
        <activity android:name=".main.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".quizbattle.activities.TestResultActivity">
        </activity>
        <activity android:name=".quizbattle.activities.TakeTestActivity">
        </activity>
        <activity android:name=".quizbattle.activities.CreateQuestionActivity">
        </activity>
        <activity android:name=".quizbattle.activities.CreateTestActivity">
        </activity>
        <activity android:name=".quizbattle.activities.QuizBattleMainActivity">
        </activity>
    </application>

</manifest>